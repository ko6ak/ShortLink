version: "1.0"
name: shortlinkapi
services:
  postgres:
    container_name: psgrs
    image: postgres:latest
    environment:
      POSTGRES_DB: "shortlink_db"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "passwd"
    ports:
      - "5432:5432"